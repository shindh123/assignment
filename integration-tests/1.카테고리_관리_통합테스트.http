### 1. 최상위 카테고리 생성 (의류)
POST http://localhost:9090/admin/categories
Content-Type: application/json

{
  "name": "의류"
}

> {%
    client.global.set("rootCategoryId", response.body.id);
    client.test("최상위 카테고리 생성 성공", function() {
        client.assert(response.status === 201, "Response status is not 201");
        client.assert(response.body.name === "의류", "Category name does not match");
    });
%}

### 2. 하위 카테고리 생성 (상의)
POST http://localhost:9090/admin/categories
Content-Type: application/json

{
  "name": "상의"
}

> {%
    client.global.set("topCategoryId", response.body.id);
    client.test("하위 카테고리(상의) 생성 성공", function() {
        client.assert(response.status === 201, "Response status is not 201");
        client.assert(response.body.name === "상의", "Category name does not match");
    });
%}

### 3. 하위 카테고리 생성 (하의)
POST http://localhost:9090/admin/categories
Content-Type: application/json

{
  "name": "하의"
}

> {%
    client.global.set("bottomCategoryId", response.body.id);
    client.test("하위 카테고리(하의) 생성 성공", function() {
        client.assert(response.status === 201, "Response status is not 201");
        client.assert(response.body.name === "하의", "Category name does not match");
    });
%}

### 4. 최하위 카테고리 생성 (티셔츠)
POST http://localhost:9090/admin/categories
Content-Type: application/json

{
  "name": "티셔츠"
}

> {%
    client.global.set("tshirtCategoryId", response.body.id);
    client.test("최하위 카테고리(티셔츠) 생성 성공", function() {
        client.assert(response.status === 201, "Response status is not 201");
        client.assert(response.body.name === "티셔츠", "Category name does not match");
    });
%}

### 5. 카테고리 단건 조회
GET http://localhost:9090/admin/categories/{{rootCategoryId}}

> {%
    client.test("카테고리 조회 성공", function() {
        client.assert(response.status === 200, "Response status is not 200");
        client.assert(response.body.id === parseInt(client.global.get("rootCategoryId")), "Category ID does not match");
        client.assert(response.body.name === "의류", "Category name does not match");
    });
%}

### 6. 카테고리 목록 조회
GET http://localhost:9090/admin/categories?page=0&size=10

> {%
    client.test("카테고리 목록 조회 성공", function() {
        client.assert(response.status === 200, "Response status is not 200");
        client.assert(response.body.content.length >= 4, "Should have at least 4 categories");
    });
%}

### 7. 카테고리 이름 변경
PATCH http://localhost:9090/admin/categories/{{topCategoryId}}
Content-Type: application/json

{
  "name": "상의 (수정됨)"
}

> {%
    client.test("카테고리 이름 변경 성공", function() {
        client.assert(response.status === 200, "Response status is not 200");
        client.assert(response.body.id === parseInt(client.global.get("topCategoryId")), "Category ID does not match");
        client.assert(response.body.name === "상의 (수정됨)", "Category name was not updated");
    });
%}

### 8. 카테고리 삭제
DELETE http://localhost:9090/admin/categories/{{tshirtCategoryId}}

> {%
    client.test("카테고리 삭제 성공", function() {
        client.assert(response.status === 204, "Response status is not 204");
    });
%}

### 9. 삭제 확인 (삭제된 카테고리 조회)
GET http://localhost:9090/admin/categories/{{tshirtCategoryId}}

> {%
    client.test("삭제된 카테고리 조회 실패", function() {
        client.assert(response.status === 404, "Response status is not 404");
    });
%}
